<UserControl x:Class="ERDiagramBuilder.MySqlConnectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ERDiagramBuilder"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="450">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Address:   " Margin="15 0"/>
            <TextBox x:Name="ipTB" Width="120" Text="{Binding Ip}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Username:" Margin="15 0"/>
            <TextBox x:Name="usernameTB" Width="120" Text="{Binding Username}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Password: " Margin="15 0"/>
            <TextBox x:Name="passwordTB" Width="120" Text="{Binding Password}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Database: " Margin="15 0"/>
            <TextBox x:Name="dbTB" Width="120" Text="{Binding Database}"/>
        </StackPanel>
        
    </StackPanel>
</UserControl>
